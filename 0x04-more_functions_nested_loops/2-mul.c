#include "holberton.h"

/**
 * mul - multiply two integers
 * @a:  firts multiplier
 * @b:  second multiplier
 * Return: On success 1. return multiply result
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
